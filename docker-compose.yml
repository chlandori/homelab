services:
  maxlab:
    image: navigatorbbs/maxlab:v0.1.2
    container_name: maxlab
    restart: unless-stopped
    ports:
      - "8888:8888"
    volumes:
      - ${NOTEBOOK_PATH:-/mnt/storage1/maxlab}:/home/maxlab/workspace
    environment:
      - GITHUB_COPILOT_PAT=${GITHUB_COPILOT_PAT:-}

