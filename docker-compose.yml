services:
  maxlab:
    image: navigatorbbs/maxlab:latest
    container_name: maxlab
    restart: unless-stopped
    ports:
      - "8888:8888"
    volumes:
      - ${NOTEBOOK_PATH:-C:/homelab/notebooks}:/home/jovyan/work
    environment:
      JUPYTER_TOKEN: ${JUPYTER_TOKEN}
